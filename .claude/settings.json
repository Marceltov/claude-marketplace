{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path // empty' | { read -r f; case \"$f\" in *.md|*.markdown) python3 \"${CLAUDE_PROJECT_DIR:-.}/.claude/hooks/unwrap-markdown.py\" --hook \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Unwrapping Markdown prose"
          }
        ]
      }
    ]
  }
}
