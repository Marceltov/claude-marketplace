---
description: Show the pull/merge request for the current branch in the terminal
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/scripts/forge.sh *)
---

!`${CLAUDE_PLUGIN_ROOT}/scripts/forge.sh pr`

The command above already printed the PR info. Do not run forge.sh again — just summarize the output shown above.
