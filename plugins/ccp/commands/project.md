---
description: Open this repo's linked GitHub Project (or GitLab issue board) in the browser
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/scripts/forge.sh *)
---

!`${CLAUDE_PLUGIN_ROOT}/scripts/forge.sh project`

The command above already opened the URL. Do not run forge.sh again — just report that it's open.
